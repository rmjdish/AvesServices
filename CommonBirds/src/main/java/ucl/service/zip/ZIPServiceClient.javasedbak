/*
    This file is part of Jay/Skylark/Swift.

    Jay/Skylark/Swift is free software: you can redistribute it and/or
    modify it under the terms of the GNU General Public License as
    published by the Free Software Foundation, either version 3 of the
    License, or (at your option) any later version.

    Jay/Skylark/Swift is distributed in the hope that it will be
    useful, but WITHOUT ANY WARRANTY; without even the implied
    warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR
    PURPOSE. See the GNU General Public License for more details.

    You should have received a copy of the GNU General Public License
    along with Jay/Skylark/Swift. If not, see
    <https://www.gnu.org/licenses/>.

*/


/**
 * Please modify this class to meet your needs
 * This class is not complete
 */

package ucl.service.zip;

import java.net.MalformedURLException;
import java.net.URL;
import java.util.logging.Logger;

import javax.xml.namespace.QName;
import jakarta.xml.ws.Service;
import jakarta.xml.ws.WebServiceClient;
import jakarta.xml.ws.WebEndpoint;
import jakarta.xml.ws.WebServiceException;
import jakarta.xml.ws.WebServiceFeature;
import jakarta.xml.ws.soap.SOAPBinding;

import mrc.util.HostInfo;
import mrc.util.Properties;
import mrc.util.Util;

/**
 * This class was generated by Apache CXF 3.3.2
 * 2019-08-13T12:53:59.849+01:00
 * Generated source version: 3.3.2
 * It was hacked about by PC trying to sort 
 * out the mess in the javax -> jakarta changes 
 * brought about by JDK 11 (March 2023)
 *
 */
@WebServiceClient(name = "ZipServiceClient", targetNamespace = "http://zip.service.ucl/")
public class ZIPServiceClient extends Service {

	public static void main(String[] args) throws Exception {
		QName serviceName = new QName("http://zip.service.ucl/", "Boomerang");
		QName portName    = new QName("http://zip.service.ucl/", "BoomerangPort");

		Service service = Service.create(serviceName);
		service.addPort(portName, SOAPBinding.SOAP11HTTP_BINDING, "http://localhost:9090/BoomerangPort");
		ucl.service.zip.ZIPService client = service.getPort(portName, ucl.service.zip.ZIPService.class);

		// Insert code to invoke methods on the client here. This is not used so don't
		// bother!
	}

	private static final Logger log = Logger.getLogger("ucl.service.zip");
	private static final String propfile = "zipservice.properties";
	private final static URL ZIPServiceClient_WSDL_LOCATION;
	private final static WebServiceException ZIPServiceClient_EXCEPTION;
	private final static QName ZIPServiceClient_QNAME = new QName("http://zip.service.ucl/", "Boomerang");
	// private final static String target =
	// "http://swantest.mrceuston.local:8984/ZIPService/services/boomerang";
	private static String target;

	static {
		Class me = ZIPServiceClient.class;
		// finds resource relative to the class location
	    URL url = me.getResource("ZIPServiceClient.class")	; // will be null if not found
	    String fpath = "./";
	    String wheretolook = propfile;
	    if ( url != null ) {
			fpath = url.getPath().replaceAll("%20", " ");
			wheretolook = fpath.substring(0, fpath.lastIndexOf("/")) + System.getProperty("file.separator") + propfile;	    	
	    } else {
	    	wheretolook = fpath + propfile;
	    }
		log.info(HostInfo.tell() + "ZIPServiceClient properties: " + wheretolook);
		Properties props = new Properties(wheretolook);
		target = props.keySearch("service_name");
		log.info(HostInfo.tell() + "ZIPServiceClient: target for web service: "+target);
		url = null;
		WebServiceException e = null;
		try {
			url = new URL(target + "?wsdl");
		} catch (MalformedURLException ex) {
			log.severe(HostInfo.tell() + " PyJobServiceClient: Error forming URL of WSDL from " + target+"?wsdl");
			e = new WebServiceException(ex);
		}
		ZIPServiceClient_WSDL_LOCATION = url;
		ZIPServiceClient_EXCEPTION = e;
	}

	// Various constructors with different signatures
	
	public ZIPServiceClient() {
		super(__getWsdlLocation(), ZIPServiceClient_QNAME);
	}

	public ZIPServiceClient(WebServiceFeature... features) {
		super(__getWsdlLocation(), ZIPServiceClient_QNAME, features);
	}

	public ZIPServiceClient(URL wsdlLocation) {
		super(wsdlLocation, ZIPServiceClient_QNAME);
	}

	public ZIPServiceClient(URL wsdlLocation, WebServiceFeature... features) {
		super(wsdlLocation, ZIPServiceClient_QNAME, features);
	}

	public ZIPServiceClient(URL wsdlLocation, QName serviceName) {
		super(wsdlLocation, serviceName);
	}

	public ZIPServiceClient(URL wsdlLocation, QName serviceName, WebServiceFeature... features) {
		super(wsdlLocation, serviceName, features);
	}

	private static URL __getWsdlLocation() {
		if (ZIPServiceClient_EXCEPTION != null) {
			throw ZIPServiceClient_EXCEPTION;
		}
		return ZIPServiceClient_WSDL_LOCATION;
	}
	/* the last two @WebEndpoint annotations were previously commented out */
    @WebEndpoint(name = "BoomerangPort")
	public ZIPService getZIPServicePort() {
		QName serviceName = new QName("http://zip.service.ucl/", "Boomerang");
		QName portName    = new QName("http://zip.service.ucl/", "BoomerangPort");
		log.info(HostInfo.tell() + " ZIPServiceClient: getZIPServicePort looking for services at target: " + target);
		return super.getPort(portName, ZIPService.class);
	}

	/**
	 * 
	 * @param features A list of {@link javax.xml.ws.WebServiceFeature} to configure
	 *                 on the proxy. Supported features not in the
	 *                 <code>features</code> parameter will have their default
	 *                 values.
	 * @return returns ZIPService
	 */
    @WebEndpoint(name = "BoomerangPort")
	public ZIPService getZIPServicePort(WebServiceFeature... features) {
		QName serviceName = new QName("http://zip.service.ucl/", "Boomerang");
		QName portName    = new QName("http://zip.service.ucl/", "BoomerangPort");
		log.info(HostInfo.tell() + " ZIPServiceClient: getZIPServicePort looking for services at target: " + target);
		return super.getPort(portName, ZIPService.class);
	}

}
